"use client"
const sendNotification = (message: string) => {
    // Log the message to the console for now. In a real application, this would be an API call.
    console.log(`Sending notification: ${message}`);
  };
  
  export default sendNotification;
  